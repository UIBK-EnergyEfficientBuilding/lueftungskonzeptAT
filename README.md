
# Setup dev venv

```
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
```

# Run dev system
```
cd infiltration
FLASK_DEBUG=1 python -m flask run
```
or use vscode run and debug
